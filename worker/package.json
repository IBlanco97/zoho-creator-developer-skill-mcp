{
  "name": "zoho-platform-upload-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "External worker that receives upload jobs from Zoho Creator and pushes documents to client CAE platforms (CTAIMA, Dokify, Nalanda, e-coordina, ...). Each job is processed by a platform-specific adapter (Playwright when no API exists).",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p .",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^17.3.1",
    "express": "^4.21.2",
    "pino": "^9.5.0",
    "pino-pretty": "^11.3.0",
    "playwright": "^1.49.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
